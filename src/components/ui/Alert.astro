---
type AlertTone = "info" | "warning" | "danger" | "success";

interface Props {
  tone?: AlertTone;
  title?: string;
  class?: string;
  [key: string]: unknown;
}

const {
  tone = "info",
  title,
  class: className = "",
  ...rest
} = Astro.props;

const baseClass =
  "rounded-[var(--radius-lg)] border border-l-4 px-4 py-3 text-sm md:px-5 md:py-4";

const toneClasses: Record<AlertTone, string> = {
  info: "border-[var(--border-strong)] bg-[var(--card)] text-[var(--foreground)]",
  warning: "border-[var(--color-warning-border)] bg-[var(--color-warning-soft)] text-[var(--color-warning-text)]",
  danger: "border-[var(--color-danger-border)] bg-[var(--color-danger-soft)] text-[var(--color-danger-text)]",
  success: "border-[var(--color-success-border)] bg-[var(--color-success-soft)] text-[var(--color-success-text)]",
};

const finalClass = [baseClass, toneClasses[tone], className].filter(Boolean).join(" ");
---

<div {...rest} class={finalClass} role="status" aria-live="polite">
  {title ? <p class="mb-1 font-semibold">{title}</p> : null}
  <div>
    <slot />
  </div>
</div>
